import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

// Placeholder: No prompts are currently registered for the Linear MCP.
// Keep this file so prompts can be added in the future without changing imports.
export function registerPrompts(_server: McpServer): void {
  // Intentionally empty
}
