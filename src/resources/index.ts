import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

// Placeholder: No resources are currently registered for the Linear MCP.
// Keep this file so resources can be added in the future without changing imports.
export function registerResources(_server: McpServer): void {
  // Intentionally empty
}
